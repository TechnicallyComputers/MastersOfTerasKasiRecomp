# Build outputs
/build/
/build-*/
/out/
/.happypath/

# Setup-host releases: players Generate game C locally (CI clears generated/).
# Keep /generated/ gitignored; regenerate with psxrecomp_cli.py / the wizard.
/generated/

# Release packaging artifacts
/release-stage/
/dist/
*.zip

# Local PlayStation assets (disc image, extracted boot EXE, memory cards)
/motk/
/isos/
/saves/
/prepared_disc/

# Disc / image / memcard formats
*.bin
*.cue
*.iso
*.img
*.sub
*.ccd
*.mcd
*.mcr
*.chd

# Ghidra projects and local analysis databases
/ghidra/projects/
*.gpr
*.rep/
*.lock
*.lock~

# Local editor / cache files
.vs/
.vscode/
*.user
*.log

# Runtime artifacts from psx-runtime launches
psx_crash.txt
psx_last_run_report.json
psx_screenshot.bmp
psx_freeze_heartbeat.json
starvation_dump.jsonl

# Diagnostic dumps from debug-server probes (root-level only)
/*.json
/*.bmp
/*.png

# Python caches
__pycache__/

# Local BIOS (symlink or copy — do not commit)
/bios/
settings.toml

# --- PSXRecomp setup-host (project studio) ---
/disc/
